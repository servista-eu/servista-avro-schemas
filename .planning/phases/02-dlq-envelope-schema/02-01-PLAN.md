---
phase: 02-dlq-envelope-schema
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/avro/dlq/DeadLetterEnvelope.avsc
autonomous: true
requirements:
  - FOUND-05

must_haves:
  truths:
    - "DeadLetterEnvelope.avsc generates a Java class at eu.servista.schemas.avro.dlq.DeadLetterEnvelope"
    - "DeadLetterEnvelope has all required fields: dlq_event_id, original_topic, original_partition, original_offset, original_key, original_value, error_type, error_message, consumer_group, retry_count, max_retries, status, correlation_id, organization_id, timestamp"
    - "DlqStatus enum has exactly two symbols: PENDING and PERMANENTLY_FAILED"
    - "Namespace is eu.servista.schemas.avro.dlq matching the dlq/ directory convention"
    - "./gradlew build passes end-to-end (codegen + compilation + tests)"
  artifacts:
    - path: "src/main/avro/dlq/DeadLetterEnvelope.avsc"
      provides: "DLQ envelope Avro schema wrapping failed messages with error metadata"
      contains: "eu.servista.schemas.avro.dlq.DeadLetterEnvelope"
  key_links:
    - from: "DeadLetterEnvelope.avsc"
      to: "servista-commons DeadLetterRouter"
      via: "DLQ routing produces DeadLetterEnvelope records serialized using this schema"
      pattern: "DeadLetterEnvelope"
    - from: "DeadLetterEnvelope.avsc"
      to: "src/main/avro/envelope/EventEnvelope.avsc"
      via: "original_value field contains the raw bytes of the failed EventEnvelope"
      pattern: "original_value"
---

<objective>
Create the DeadLetterEnvelope Avro schema for DLQ message wrapping in the servista-avro-schemas repository. This schema wraps original failed message bytes with error metadata (error_type, error_message, original_topic, consumer_group, retry_count, status, correlation context, and timestamp). After this phase, build and publishToMavenLocal so the downstream servista-commons DLQ routing can use the generated DeadLetterEnvelope class.

Purpose: Provide the DLQ envelope schema as part of the centralized Avro schema artifact. The generated `eu.servista.schemas.avro.dlq.DeadLetterEnvelope` class is consumed by servista-commons' DeadLetterRouter to wrap failed messages before routing them to DLQ topics.

Output: `DeadLetterEnvelope.avsc` in the `dlq/` directory alongside existing domain directories, generating a Java class. Build passes with codegen + compilation + existing tests.
</objective>

<execution_context>
@/home/sven/.claude-pp/get-shit-done/workflows/execute-plan.md
@/home/sven/.claude-pp/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-dlq-envelope-schema/02-CONTEXT.md
@.planning/phases/02-dlq-envelope-schema/02-RESEARCH.md

**Target repo:** This repo (`servista-avro-schemas`)

<interfaces>
<!-- Existing schema structure to match -->

Existing Avro schema directory structure:
```
src/main/avro/
  accounts/      AccountCreated.avsc
  authorization/ .gitkeep
  billing/       .gitkeep
  egress/        .gitkeep
  envelope/      EventEnvelope.avsc
  iam/           .gitkeep
  integrations/  .gitkeep
  organizations/ OrgCreated.avsc
  subscriptions/ .gitkeep
  usage/         .gitkeep
```
DLQ schema goes in a new `dlq/` directory alongside `envelope/`.

Existing EventEnvelope schema (sibling pattern to follow):
```json
{
  "namespace": "eu.servista.schemas.avro.envelope",
  "type": "record",
  "name": "EventEnvelope",
  "fields": [
    {"name": "event_id", "type": "long"},
    {"name": "event_type", "type": "string"},
    {"name": "aggregate_id", "type": "long"},
    {"name": "organization_id", "type": "long"},
    {"name": "account_id", "type": ["null", "long"], "default": null},
    {"name": "timestamp", "type": "long"},
    {"name": "correlation_id", "type": "string"},
    {"name": "causation_id", "type": ["null", "long"], "default": null},
    {"name": "payload", "type": "bytes"}
  ]
}
```

Convention: namespace `eu.servista.schemas.avro.{directory}` must match the parent directory name. This is enforced by `SchemaNamespaceConsistencyTest`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create DeadLetterEnvelope Avro schema</name>
  <files>
    src/main/avro/dlq/DeadLetterEnvelope.avsc
  </files>
  <behavior>
    - DeadLetterEnvelope.avsc has all required fields: dlq_event_id (long), original_topic (string), original_partition (int), original_offset (long), original_key (union null/bytes, default null), original_value (bytes), error_type (string), error_message (string), consumer_group (string), retry_count (int), max_retries (int), status (DlqStatus enum with PENDING and PERMANENTLY_FAILED), correlation_id (union null/string, default null), organization_id (union null/long, default null), timestamp (long)
    - DlqStatus enum has exactly two symbols: PENDING and PERMANENTLY_FAILED
    - Namespace is eu.servista.schemas.avro.dlq (matches dlq/ directory for namespace consistency test)
    - Schema has doc strings on all fields
    - ./gradlew build passes (Avro codegen generates the Java class, compilation succeeds, all existing tests still pass including SchemaNamespaceConsistencyTest)
  </behavior>
  <action>
Create `src/main/avro/dlq/DeadLetterEnvelope.avsc` with this exact schema:

```json
{
  "namespace": "eu.servista.schemas.avro.dlq",
  "type": "record",
  "name": "DeadLetterEnvelope",
  "doc": "Wraps a failed message with error metadata for DLQ routing. Registered in Apicurio with FULL compatibility.",
  "fields": [
    {"name": "dlq_event_id",      "type": "long",                        "doc": "Snowflake ID for this DLQ entry"},
    {"name": "original_topic",    "type": "string",                      "doc": "Source topic the message was consumed from"},
    {"name": "original_partition", "type": "int",                        "doc": "Partition number in source topic"},
    {"name": "original_offset",   "type": "long",                        "doc": "Offset in source topic partition"},
    {"name": "original_key",      "type": ["null", "bytes"],             "default": null, "doc": "Original message key bytes"},
    {"name": "original_value",    "type": "bytes",                       "doc": "Original message value bytes (full EventEnvelope)"},
    {"name": "error_type",        "type": "string",                      "doc": "Exception class name or error category"},
    {"name": "error_message",     "type": "string",                      "doc": "Human-readable error description"},
    {"name": "consumer_group",    "type": "string",                      "doc": "Consumer group that failed to process"},
    {"name": "retry_count",       "type": "int",                         "doc": "Number of processing attempts before DLQ routing"},
    {"name": "max_retries",       "type": "int",                         "doc": "Configured max retry limit"},
    {"name": "status",            "type": {"type": "enum", "name": "DlqStatus", "symbols": ["PENDING", "PERMANENTLY_FAILED"]}, "doc": "Whether message is eligible for reprocessing"},
    {"name": "correlation_id",    "type": ["null", "string"],            "default": null, "doc": "Correlation ID from original EventEnvelope if extractable"},
    {"name": "organization_id",   "type": ["null", "long"],              "default": null, "doc": "Organization ID from original EventEnvelope if extractable"},
    {"name": "timestamp",         "type": "long",                        "doc": "Unix epoch millis when DLQ entry was created"}
  ]
}
```

The `dlq/` directory follows the same convention as other domain directories (`accounts/`, `envelope/`, etc.). The namespace `eu.servista.schemas.avro.dlq` matches the directory name, satisfying the SchemaNamespaceConsistencyTest.
  </action>
  <verify>
    <automated>./gradlew build --no-daemon 2>&1 | tail -20</automated>
  </verify>
  <done>
    - DeadLetterEnvelope.avsc exists in src/main/avro/dlq/ with all 15 fields and DlqStatus enum
    - Avro codegen generates eu.servista.schemas.avro.dlq.DeadLetterEnvelope Java class
    - ./gradlew build passes (codegen + compilation + all tests including namespace consistency)
  </done>
</task>

</tasks>

<verification>
1. DeadLetterEnvelope.avsc exists in src/main/avro/dlq/ with all required fields and DlqStatus enum
2. Avro codegen generates eu.servista.schemas.avro.dlq.DeadLetterEnvelope Java class
3. SchemaNamespaceConsistencyTest passes (namespace matches dlq/ directory)
4. SchemaCompatibilityTest passes (no regressions)
5. ./gradlew build passes end-to-end
</verification>

<success_criteria>
- DLQ envelope Avro schema committed to src/main/avro/dlq/DeadLetterEnvelope.avsc
- All 15 fields present with correct types and doc strings
- DlqStatus enum with PENDING and PERMANENTLY_FAILED symbols
- Build passes with all existing tests
- Generated DeadLetterEnvelope class available at eu.servista.schemas.avro.dlq.DeadLetterEnvelope
</success_criteria>

<notes>
**Post-phase action:** After this phase completes successfully, run `./gradlew publishToMavenLocal` so that the downstream servista-commons repo can import `eu.servista.schemas.avro.dlq.DeadLetterEnvelope` when building the DeadLetterRouter and ServistaKafkaConsumer (governance Phase 6, servista-commons local phase).
</notes>

<output>
After completion, create `.planning/phases/02-dlq-envelope-schema/02-01-SUMMARY.md`
</output>
